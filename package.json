{
  "name": "museum",
  "version": "1.0.0",
  "scripts": {
    "build": "rm -rf ./public/images/* && cp ~/Desktop/museum/* ./public/images/ 2>/dev/null; touch ./public/images/.gitkeep; node build.js && mkdir -p dist && cp -r public/* dist/",
    "deploy": "npm run build && git add . && git commit -m 'sync' || true && vercel --prod"
  },
  "dependencies": {
    "axios": "^1.13.4",
    "form-data": "^4.0.5"
  }
}
